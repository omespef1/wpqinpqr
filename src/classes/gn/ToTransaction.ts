
export interface ToTransaction{
    retorno:number;
    txtRetorno:string,
    objTransaction:any
  }

  export interface Transaction {
    Retorno:number;
    TxtRetorno:string,
    ObjTransaction:any
  }
