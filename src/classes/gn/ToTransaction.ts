
export interface ToTransaction{
    retorno:number;
    txtRetorno:string,
    objTransaction:any
  }

  export interface Transaction {
    Retorno:number;
    TxtRetorno:string,
    ObjTransaction:any
  }

  export interface Transaction2 {
    Retorno:number,
    TxtError:string,
    ObjTransaction:any
  }
